services:
 api:
  build: .
  ports: ["8000:8000"]
  env_file: .env
  depends_on: [db]
 db:
  image: pgvector/pgvector:pg17
  environment:
   POSTGRES_DB: document_ai
   POSTGRES_USER: postgres
   POSTGRES_PASSWORD: postgres
  ports: ["5432:5432"]
